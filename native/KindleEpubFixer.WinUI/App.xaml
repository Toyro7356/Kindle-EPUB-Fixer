<Application
    x:Class="KindleEpubFixer.WinUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
            <FontFamily x:Key="AppFontFamily">Microsoft YaHei UI</FontFamily>
            <SolidColorBrush x:Key="CardBackgroundBrush" Color="#B8FFFFFF" />
            <SolidColorBrush x:Key="SubtleBorderBrush" Color="#BFD0D7DE" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#5F6872" />
            <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="#D6F4F7F5" />
            <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="#D6F4F7F5" />
            <SolidColorBrush x:Key="NavigationViewTopPaneBackground" Color="#D6F4F7F5" />
            <SolidColorBrush x:Key="TableHeaderBrush" Color="#44FFFFFF" />
            <SolidColorBrush x:Key="TableRowHoverBrush" Color="#55FFFFFF" />
            <SolidColorBrush x:Key="ColumnDividerBrush" Color="#667B8490" />
            <SolidColorBrush x:Key="ColumnDividerHoverBrush" Color="#8A5F6872" />
            <Style x:Key="ColumnResizeThumbStyle" TargetType="Thumb">
                <Setter Property="Width" Value="8" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Grid Background="Transparent">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Line.Fill" Value="{StaticResource ColumnDividerHoverBrush}" />
                                                <Setter Target="Line.Opacity" Value="0.72" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Line.Fill" Value="{StaticResource ColumnDividerHoverBrush}" />
                                                <Setter Target="Line.Opacity" Value="0.95" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Rectangle x:Name="Line" Width="1" Fill="{StaticResource ColumnDividerBrush}" HorizontalAlignment="Center" Opacity="0.18" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
